/**
 * image-hosting provider entry.
 *
 * Today image-hosting providers are primarily supplied by plugins.
 * This file exists to keep provider directory structure consistent
 * with other provider kinds (OpenSpec: model-providers).
 */

export type { ImageHostingProvider, ImageHostingUploadOptions, ImageHostingUploadResult } from './types';

